# assets

Drop the demo capture here as **`demo.gif`** — it is referenced at the top of the root
`README.md`. Record the recall moment: hook (nonna) → split-screen → HUD writing the ledger →
recall (base flounders, Whisperer answers) → the number `0/10 · 10/10`.
